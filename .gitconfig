[user]
	name = kandaharu
	email = yuugengaisha@gmail.com
[color]
	ui = auto
[alias]
	plog = log --graph --decorate=short --branches --date=iso --pretty
	pslog = log --graph --decorate=short --branches --date=iso --pretty --stat=100,100
	dlog = log --graph --decorate=short --branches --date=short --pretty=format:'%Cgreen%h %Creset%cd %Cblue%cn %Cred%d %Creset%s'
	dslog = log --graph --decorate=short --branches --date=short --pretty=format:'%Cgreen%h %Creset%cd %Cblue%cn %Cred%d %Creset%s' --stat=100,100
	co = checkout
	ci = commit
	reb = rebase
	st = status
	s = status -s
	df = diff
	br = branch
	ff = flow feature
	fh = flow hotfix
	fr = flow release
[core]
	editor = 'vim' -c 'set fileencoding=utf-8'
[pager]
	log = diff-highlight | less
	show = diff-highlight | less
	diff = diff-highlight | less
[merge]
	ff = false
[pull]
	rebase = true
[push]
	default = simple
